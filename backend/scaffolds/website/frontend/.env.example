# Base URL the browser uses to reach the API.
# - Local dev: the Vite proxy handles /api, so leave this unset.
# - Separate deployment: set it to the backend's public origin + /api.
VITE_API_BASE=

# Optional: override the dev-time proxy target for the backend.
VITE_DEV_PROXY=http://localhost:8080
